/** @type {const} */
const themeColors = {
  primary: { light: '#00D4FF', dark: '#00D4FF' },       // Cyan neon - JARVIS accent
  background: { light: '#0A0E1A', dark: '#0A0E1A' },   // Deep dark blue-black
  surface: { light: '#111827', dark: '#111827' },       // Slightly lighter dark
  foreground: { light: '#E0F7FF', dark: '#E0F7FF' },   // Light cyan-white text
  muted: { light: '#4A90A4', dark: '#4A90A4' },        // Dim cyan for secondary text
  border: { light: '#1E3A5F', dark: '#1E3A5F' },       // Dark blue border
  success: { light: '#00FF88', dark: '#00FF88' },      // Green neon
  warning: { light: '#FFB800', dark: '#FFB800' },      // Amber
  error: { light: '#FF3366', dark: '#FF3366' },        // Red-pink neon
  accent: { light: '#0088FF', dark: '#0088FF' },       // Blue accent
};

module.exports = { themeColors };
